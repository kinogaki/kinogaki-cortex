# Giving the workspace something to do

**Experiment AL · 2026-06-26 · partial (win + honest negative)**

The System-2 serial workspace **reaches a 2-hop target (acc 1.00)** where System 1 and
one-step deferral both score **0.00** (trapped on the intermediate) — reachability is
real and new. But on a single deterministic chain it only ties a blind "apply-twice";
the focus/inhibition machinery is built to **select among competing chains**, which this
probe lacks. Next axis named: competing candidate chains.

**Run it** (from the repo root, after `bash data/get-data.sh`):

```sh
python experiments/multi-step-workspace/run.py
```

**Blog post:** ["Giving the workspace something to do"](https://cortex.kinogaki.com/blog/multi-step-workspace/)
