# When a habit becomes a thought

**Experiment AH · 2026-06-26 · qualified win**

Stability — not error — promotes a mastered construction from an implicit count
into an explicit, slot-addressable concept that answers queries the flat count
structurally cannot: inverted slot lookup, role substitution, slot analogy. The
Karmiloff-Smith U-shaped dip is confirmed: next-word accuracy 0.155 → trough 0.051
→ recovered 0.181 (above the implicit baseline). 5,217 constructions promoted on
mastery, single pass. The honest caveat: the recovery is a consequence of the
modeled E1→E2 re-binding (a frozen snapshot dips forever) — the finding is that
the KS mechanism is expressible and self-consistent on counts, with
manipulability the real win. These explicit concepts are the operands the
System-2 deliberate pass needs.

**Run it** (from the repo root, after `bash data/get-data.sh`):

```sh
python experiments/redescription/run.py
```

**Blog post:** ["When a habit becomes a thought"](https://cortex.kinogaki.com/blog/redescription/)
