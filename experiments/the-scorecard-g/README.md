# Building the dials that bits-per-char hides (G)

**Experiment GH · 2026-06-25 · win**

The first half of the scorecard work (Exp G): builds better rulers than bits-per-char — overfitting gap, real-word rate, phrase coherence. Pairs with `the-scorecard-h`. Headline: each concept level buys exactly what it models, visible on the new dials where bits-per-char was blind; and every model, even the best, still produced locally-plausible word-salad — naming the global-coherence frontier.

**Run it** (from the repo root, after `bash data/get-data.sh`):

```sh
python experiments/the-scorecard-g/run.py
```

**Blog post:** ["Building the dials that bits-per-char hides (G)"](https://cortex.kinogaki.com/the-scorecard/)
